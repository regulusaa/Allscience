# Allscience — early prototype

An earlier experiment in building a science revision website with plain HTML and CSS. It contains subject pages, images, and several unfinished pages.

Open `index.html` in a browser to explore this version. The newer project lives in the separate `all-science` repository.
